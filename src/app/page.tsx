// Root page is handled by src/app/(app)/page.tsx (Dashboard).
// This file intentionally left as an empty export to satisfy Next.js file conventions.
// The (app) route group owns the / route with its sidebar layout.
export { default } from '@/app/(app)/page'
